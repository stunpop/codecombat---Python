while True:
    enemy = hero.findNearestEnemy()
    distance = hero.distanceTo(enemy)
    if distance < 10:
        hero.attack(enemy)
        pass
    # Else, stay close to the peasant! Use else.
    if distance > 10:
       hero.moveXY(40, 37) 
    
